package dev.lavalinkplugins.golibrespot;

import dev.arbjerg.lavalink.api.PluginEventHandler;
import org.springframework.stereotype.Service;

/**
 * Lavalink plugin entrypoint for the go-librespot adapter.
 *
 * <p>Compile-only skeleton for the build scaffold (T2). The real wiring — Spring
 * configuration binding, the {@code AudioSourceManager} bean registration, and the
 * player lifecycle bridge — lands in the plugin-registration todo (T19).</p>
 */
@Service
public class GoLibrespotPlugin extends PluginEventHandler {
}
