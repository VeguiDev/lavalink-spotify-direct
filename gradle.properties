# Gradle daemon JVM arguments
org.gradle.jvmargs=-Xmx1g -Dfile.encoding=UTF-8

# Only fail the build if there is a dependency version conflict
org.gradle.warning.mode=summary

# Reproducible builds (timestamps/order are normalized by the Jar tasks anyway)
org.gradle.caching=true
